class Solution {
	public:
	int lowerBound(vector<int>& arr, int target) {
		// code here
		int st = 0;
		int ans = arr.size();
		int end = arr.size() - 1;
		while (st <= end) {
			int mid = (st + end)/2;
			if (arr[mid] >= target) {
				end = mid - 1;
				ans = mid;
			}
			else {
				st = mid + 1;
				
			}
		}
		return ans;
	}
};
