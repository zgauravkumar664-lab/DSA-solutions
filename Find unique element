class Solution {
	public:
	int findUnique(int k, vector<int>& arr) {
		// code here
		map<int, int>mp;
		for (int i = 0; i<arr.size(); i++) {
			mp[arr[i]]++;
		}
		for (auto i:mp) {
			if (i.second != k) {
				return i.first;
			}
		}
	}
};
