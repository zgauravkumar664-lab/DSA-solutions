int countDistinct(vector<int>& arr) {
    // code here
    map<int,int>mp;
    int count=0;
    for(int i=0;i<arr.size();i++){
        mp[arr[i]]++;
    }
    for(auto i:mp){
        count++;
    }
    return count;
}
