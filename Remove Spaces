class Solution {
  public:
    string removeSpaces(string& s) {
        // code here
        for(int i=0;i<s.size();i++){
            if(s[i]==' '){
                s.erase(s.begin()+i);
                i--;
            }
        }
        return s;
    }
};
