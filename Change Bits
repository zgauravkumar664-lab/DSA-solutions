class Solution {
	public:
	vector<int> changeBits(int n) {
		// code here
		int temp = n;
		if (temp == 0) {
			return {1, 1};
		}
		int count = 0;
		int ans = 0;
		while (n != 0) {
			ans = ans + pow(2, count++);
			n = n>> 1;
		}
		
		return {ans - temp, ans};
	}
};
